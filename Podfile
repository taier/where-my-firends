# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'where-my-frieds' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   
  pod 'Alamofire', '~> 4.7', :inhibit_warnings => true
  pod 'Kingfisher', '~> 4.0', :inhibit_warnings => true 
  pod 'GoogleMaps', :inhibit_warnings => true
  pod 'GooglePlaces', :inhibit_warnings => true 
  pod 'SwiftSoup', :inhibit_warnings => true 
  pod 'SQLite.swift', '~> 0.11.5', :inhibit_warnings => true 

  # Pods for where-my-frieds

end
